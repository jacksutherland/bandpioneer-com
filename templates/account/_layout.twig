{% extends '_layout2023' %}

{% set allowAnonymous = allowAnonymous is defined ? allowAnonymous : false %}

{% if not allowAnonymous and not currentUser %}
	{% redirect '/?login=true' %}
{% endif %}

{% set versionNumber = 'rockstars-1.003' %}
{% set error = craft.app.session.getFlash('error') %}
{% set notice = craft.app.session.getFlash('notice') %}

{% block styles %}
    <link rel="stylesheet" href="{{ url('css/site2023.min.css') }}?version={{ versionNumber }}" /> 
    <link rel="stylesheet" href="{{ url('css/rockstars.css') }}?version={{ versionNumber }}" /> 
{% endblock %}